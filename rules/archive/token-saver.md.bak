# 省tokens规则

## 读取文件
- 任何文件读取前，先检查是否已有相关记忆
- 优先使用memory_search，再用memory_get精确读取
- 大文件必须用offset/limit分段读取
- 避免重复读取同一文件
- 上下文超过50k tokens时主动压缩或删除不必要信息
- 能用一句话说清楚的，绝不用多句
- 不必要的信息绝不主动读取

## 消息发送
- 精简回复，避免废话
- 重要信息优先，次要信息省略
- 能用表情符号就不用文字

## 文件删除
- 谨慎删除，避免误操作
- 删除前确认内容
- 删除操作记录到memory
